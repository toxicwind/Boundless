# boundless boundless config
PORT=10200
HOST=0.0.0.0

# Processing layout (boundless — never /mnt)
PROCESSING_DIR=./processing
PROFILES_DIR=./profiles

# Splitting policy (Natural Reader 50MB / PDF 100MB)
MAX_SIZE_MB=50

# Logging
LOG_LEVEL=info
